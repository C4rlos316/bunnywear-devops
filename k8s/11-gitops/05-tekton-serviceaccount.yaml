apiVersion: v1
kind: ServiceAccount
metadata:
  name: tekton-build-sa
  namespace: tekton-pipelines

secrets:
  - name: dockerhub-secret
  - name: git-credentials

imagePullSecrets:
  - name: dockerhub-secret
