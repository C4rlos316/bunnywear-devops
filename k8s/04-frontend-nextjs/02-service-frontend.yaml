apiVersion: v1
kind: Service
metadata:
  name: bunnywear-frontend
  namespace: bunnywear

spec:
  type: NodePort

  selector:
    app: bunnywear-frontend

  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
