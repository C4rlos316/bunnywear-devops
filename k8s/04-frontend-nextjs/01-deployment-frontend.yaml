apiVersion: apps/v1
kind: Deployment
metadata:
  name: bunnywear-frontend
  namespace: bunnywear

spec:
  replicas: 1

  selector:
    matchLabels:
      app: bunnywear-frontend

  template:
    metadata:
      labels:
        app: bunnywear-frontend

    spec:
      containers:
      - name: frontend
        image: docker.io/c4rlos316/bunnywear-frontend:c7e81fba63a13cc1a517fc6699ce3fb3ad02d55c
        imagePullPolicy: Always

        env:
        - name: NEXT_PUBLIC_API_URL
          value: "http://django-backend:8000"

        ports:
        - containerPort: 3000
